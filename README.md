# CK-IN-OUT

pip install flask flask_sqlalchemy
pip install requests

CK-IN-OUT/              <-- โฟลเดอร์หลัก
│
├── app.py               <-- ไฟล์ Python (Backend) 
│
├── templates/           <-- (โฟลเดอร์) เก็บหน้าเว็บ
│   ├── index.html       <-- หน้า User
│   └── admin.html       <-- หน้า Admin
│
└── static/              <-- (โฟลเดอร์) เก็บไฟล์ตกแต่งและสคริปต์
    ├── style.css        <-- ไฟล์ CSS
    ├── script.js        <-- ไฟล์ JS
    └── img/             <-- (โฟลเดอร์) เก็บรูปภาพ
        └── logo.png     <-- รูปโลโก้มหาวิทยาลัย


python app.py < -- คำสั่งรั่น server
ได้ครับ เพื่อให้คุณ (หรือคนที่จะมารับงานต่อ) สามารถติดตั้งและรันระบบนี้ได้ทันที ผมสรุปเป็น คู่มือการติดตั้ง (Installation Guide) แบบทีละขั้นตอนให้ครับ

📘 คู่มือการติดตั้งระบบ Lab Check-in (Flask Web App)
ระบบนี้พัฒนาด้วยภาษา Python โดยใช้ Framework ชื่อ Flask ทำงานร่วมกับฐานข้อมูล SQLite (สร้างเองอัตโนมัติ) และหน้าเว็บ HTML/CSS/JS

📌 สิ่งที่ต้องเตรียม (Prerequisites)
คอมพิวเตอร์ (Windows, Mac, หรือ Linux)

Internet (จำเป็นต้องใช้ตอนติดตั้งโปรแกรม และตอนรันเพื่อดึงกราฟและ QR Code)

ขั้นตอนที่ 1: ติดตั้ง Python
ถ้าในเครื่องยังไม่มี Python ให้ดาวน์โหลดและติดตั้งก่อน

ไปที่เว็บ python.org

ดาวน์โหลดเวอร์ชันล่าสุด (แนะนำ 3.10 ขึ้นไป)

สำคัญมาก: ตอนกดติดตั้ง ให้ติ๊กถูกช่อง ☑️ Add Python to PATH (ไม่งั้นจะพิมพ์คำสั่งไม่ได้)

กด Install Now จนเสร็จ

ขั้นตอนที่ 2: จัดเตรียมโฟลเดอร์โปรเจกต์
สร้างโฟลเดอร์ใหม่ (เช่นชื่อ Lab_System) และวางไฟล์ตามโครงสร้างนี้ เป๊ะๆ (ห้ามผิดแม้แต่ตัวอักษรเดียว):

Plaintext

Lab_System/              <-- โฟลเดอร์หลัก
│
├── app.py               <-- ไฟล์ Python (Backend) ที่เราทำกันล่าสุด
│
├── templates/           <-- (โฟลเดอร์) เก็บหน้าเว็บ
│   ├── index.html       <-- หน้า User
│   └── admin.html       <-- หน้า Admin
│
└── static/              <-- (โฟลเดอร์) เก็บไฟล์ตกแต่งและสคริปต์
    ├── style.css        <-- ไฟล์ CSS
    ├── script.js        <-- ไฟล์ JS
    └── img/             <-- (โฟลเดอร์) เก็บรูปภาพ
        └── logo.png     <-- รูปโลโก้มหาวิทยาลัย
หมายเหตุ: รูป logo.png คุณต้องหาไฟล์รูป .png มาวางใส่เองนะครับ

ขั้นตอนที่ 3: ติดตั้ง Library ที่จำเป็น
เปิด Command Prompt (CMD) หรือ Terminal แล้วพิมพ์คำสั่งต่อไปนี้ทีละบรรทัด (กด Enter หลังจบแต่ละบรรทัด):

เข้าไปที่โฟลเดอร์โปรเจกต์ (ตัวอย่างถ้าโฟลเดอร์อยู่ Desktop):

Bash

cd Desktop/Lab_System
ติดตั้งตัวจัดการเว็บ (Flask) และฐานข้อมูล:

Bash

pip install flask flask_sqlalchemy
ติดตั้งตัวยิง API (Requests):

Bash

pip install requests
ขั้นตอนที่ 4: เริ่มรันระบบ (Start Server)
เมื่อติดตั้งทุกอย่างเสร็จแล้ว ให้พิมพ์คำสั่งนี้ใน CMD เพื่อเริ่มทำงาน:

Bash

python app.py

ถ้าสำเร็จ จะขึ้นข้อความประมาณนี้:
 * Serving Flask app 'app'
 * Debug mode: on
 * Running on http://127.0.0.1:5000
(ห้ามปิดหน้าต่าง CMD นี้ทิ้งนะครับ ถ้าปิด เว็บจะดับ)
